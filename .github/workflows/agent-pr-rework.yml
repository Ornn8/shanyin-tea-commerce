name: Agent PR Rework

# Configure DSH_AUTOMATION_CI_WORKFLOWS before enabling CI repair requests.

on:
  issue_comment:
    types: [created]
  repository_dispatch:
    types: [dsh-repair, agent_work_requested]

permissions:
  actions: write
  contents: read
  issues: write
  pull-requests: write

jobs:
  dsh-comment-rework:
    if: >-
      github.event_name == 'issue_comment'
      && github.event.issue.pull_request
      && contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.comment.author_association)
      && (startsWith(github.event.comment.body, '@dsh ')
        || startsWith(github.event.comment.body, 'DSH: ')
        || startsWith(github.event.comment.body, '@agent ')
        || startsWith(github.event.comment.body, 'agent: ')
        || startsWith(github.event.comment.body, '/automation '))
    uses: Ornn8/dsh-agent-automation/.github/workflows/wake-rework.yml@a635add80f0e1c71ed72622e0dacf89d87223e4f
    with:
      pull_request_number: ${{ github.event.issue.number }}
      comment_id: ${{ github.event.comment.id }}

  dsh-repair:
    if: >-
      github.event_name == 'repository_dispatch'
          && (github.event.action == 'dsh-repair'
        || (github.event.action == 'agent_work_requested'
          && github.event.client_payload.version == 2
          && github.event.client_payload.role == 'change'
          && github.event.client_payload.workflowId == 'repair'
          && github.event.client_payload.stageId == 'change'
          && github.event.client_payload.repository == github.repository
          && github.event.client_payload.subject.type == 'pull-request'))
    uses: Ornn8/dsh-agent-automation/.github/workflows/dsh-repair.yml@a635add80f0e1c71ed72622e0dacf89d87223e4f
    with:
      pull_request_number: ${{ github.event.action == 'agent_work_requested' && github.event.client_payload.subject.number || github.event.client_payload.pr_number }}
      head_sha: ${{ github.event.action == 'agent_work_requested' && github.event.client_payload.revision.head || github.event.client_payload.head_sha }}
      request_id: ${{ github.event.action == 'agent_work_requested' && github.event.client_payload.requestId || github.event.client_payload.request_id }}
      ci_workflow_name: ${{ github.event.client_payload.ci_workflow_name || '' }}
      work_request: ${{ github.event.action == 'agent_work_requested' && toJSON(github.event.client_payload) || '' }}
